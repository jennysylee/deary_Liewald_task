# deary_Liewald_task
Training blocks (8 trials simple, 8 trials choice) with feedback (wrong_key, too_slow, reminder).

Real blocks (20 simple, 40 choice) without feedback.

4-choice positions: left-to-right (-150, -50, +50, +150), mapped to Z, X, , (comma), . (period) keys.

Random foreperiod: 1000–3000ms.

Timeout: 3000ms.
